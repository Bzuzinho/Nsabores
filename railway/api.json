{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm --filter @nsabores/api build"
  },
  "deploy": {
    "startCommand": "sh -c 'if [ -z \"${BOOTSTRAP_ADMIN_EMAIL:-}\" ]; then unset BOOTSTRAP_ADMIN_EMAIL; fi; if [ -z \"${BOOTSTRAP_ADMIN_PASSWORD_HASH:-}\" ]; then unset BOOTSTRAP_ADMIN_PASSWORD_HASH; fi; exec pnpm --filter @nsabores/api start:prod'",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
